/**
 * Shared presentational primitives used across multiple pages.
 * Re-exported here so consuming modules can import from a single entry point.
 */
export { Card, CardHeader, CardContent, CardFooter } from './Card';
export { Button, IconButton } from './Button';
export { Section, SectionHeader } from './Section';
export { FormField, Input, Select, ColorInput } from './FormField';
